# Tests

Use this directory for cross-language or integration-style tests as the script catalog grows. Python package tests currently live under `python/tests/` so `cd python && uv run pytest` works directly.
