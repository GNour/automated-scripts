# CLAUDE.md

<!-- Copy to the app repo root as CLAUDE.md, next to AGENTS.md.
     Claude Code reads CLAUDE.md only; the canonical handbook lives in
     AGENTS.md (the cross-agent standard Codex reads natively). The line
     below is the official import pattern — keep this file to the import
     plus, if ever needed, Claude-only notes. Delete these comments. -->

@AGENTS.md
