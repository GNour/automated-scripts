# Python Project

This uv project will hold typed Python CLIs for the scripts catalog. No runnable CLIs are scaffolded yet.

## Checks

```bash
uv sync
uv run pytest
uv run ruff check .
uv run mypy src
```
