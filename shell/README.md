# Shell Scripts

Place bash/POSIX entrypoints here when a task adds real script behavior. Shared helpers belong in `shell/lib/` and scripts should source them by repo-relative path.

No executable scripts are scaffolded yet.
